<div class="row">
  <aside class="col-md-4">
    <%= render 'sidebar_user' %>
    <div class="btn-group-vertical" role="group">
      <button type="button" class="btn btn-info btn-lg active">
        Following Users
      </button>
      <%= link_to "Followed Users", followers_user_path(@user),
        class: "btn btn-link btn-lg"%>
    </div>
  </aside>
  <% if logged_in? %>
    <div class="col-md-8">
      <h3>Following Users (<%= @relational_users.count %>)</h3>
      <% if @relational_users.any? %>
        <%= render 'relational_users' %>
      <% end %>
    </div>
  <% end %>
</div>
