<%= form_for(current_user.following_relationships.find_by(followed_id: unfollow_user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>
